import { Team } from "./team";

export class Tournament {
  constructor(
    tournamentId: number,
    tournamentName: string,
    state: string,
    tournamentFormate: string,
    team: Team[]
  ) {}
}
